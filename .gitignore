# College ERP

# Node
node_modules/
.npm

# Next.js
.next/
out/
build/

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Supabase local
supabase/.branches
supabase/.temp

# Misc
.DS_Store
*.pem
Thumbs.db

# Build artifacts
tsconfig.tsbuildinfo

# Demo credentials — contains passwords, never commit
DEMO_CREDENTIALS.txt

# AutoClaw KDream data
.autoclaw/

# Stray/local files — not meant for the repo
hi.txt
master_prompt.md
ui_masterPrompt.md

# AI tool configs (delete these two lines if you WANT them tracked in git)
.agent/
.clinerules/
.continue/
